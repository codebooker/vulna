# VulnaForge

Scanner plugin SDK: manifest schema, parser contracts, and the community
integration framework. The canonical manifest schema is
`shared/schemas/plugin.schema.json`. Adapters land in `scout/plugins/` starting
in **Phase 4**. See `VULNA_CODEX_BUILD_PLAN.md` §12.
